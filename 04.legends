
import numpy as np
import matplotlib.pyplot as plt

# Data
a = np.arange(5)
b = [2, 4, 6, 8, 10]
c = [5, 6, 7, 8, 9]

fig = plt.figure()
ax = plt.subplot()
ax.plot(a, b, 'k--', label='Frequency')
ax.plot(a, c, 'k:', label='Periods')
#ax.legend() #center a hobe


$legend=ax.legend(loc='upper center')
#front size
#legend=ax.legend(loc='upper center',fontsize='xx-large')

legend.get_frame().set_facecolor('red')#legend background color

plt.title("Frequency of a Signal")
plt.show()
